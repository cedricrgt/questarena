import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["randomuser.me", "via.assets.so", "www.worldhistory.org", "placehold.co", "example.com"],
  },
};

export default nextConfig;
